# Unit Test Logical Path Coverage Review — PilotApiDotNet

- **Run ID**: 20260917-120000
- **Generated**: 2026-09-17 (local)
- **Target root**: `C:\Working\Storage\Dev\GitHub\PilotApiDotNet` (external directory, analyzed strictly read-only)
- **Tool chain**: Unit Test Coverage Reviewer agent → `unit-test-coverage-test-mapping` → `unit-test-coverage-control-flow-extraction` → `unit-test-coverage-test-assertion-audit` → `unit-test-coverage-reporting`
- **Transition artifacts**: `transitions/run-20260917-120000.md`, `01-test-mapping-20260917-120000.md`, `02-control-flow-20260917-120000.md`, `03-assertion-audit-20260917-120000.md`

> **Diagnostic only.** No source or test files were modified. All remediation below is suggestion-only, as Given/When/Then skeletons for developer review.

---

## 1. Executive Summary

| Metric | Value |
|--------|-------|
| Target root | `C:\Working\Storage\Dev\GitHub\PilotApiDotNet` |
| Solution | PilotApi.sln — layered .NET Web API (Domain → Repositories → Services → Shared → Web), NUnit + Moq test stack |
| Source units analyzed (with logic) | 88 (of 134 .cs files; 41 interfaces + 5 constant-only files excluded from path analysis) |
| Test suites mapped | 84 unit suites (+9 architecture/convention suites tracked separately, 1 Bruno E2E collection excluded) |
| Total logical paths ($N_{\text{total}}$) | 745 |
| Covered (strong assertions) | 250 (33.6%) |
| Partially Covered (weak assertions) | 56 (7.5%) |
| Uncovered | 439 (58.9%) |
| Mock Bypass risks | 2 (both latent) |
| Total problems ($N_{\text{problems}}$) | 497 |
| **Problem percentage** | **66.7%** |
| Critical / High / Medium / Low | 136 / 191 / 76 / 94 |

**Headline finding**: the suite excels at static/model-level verification (DTOs, entities, configuration validation, security helpers, mappers, SQL builder guards — strong, exact assertions) but never executes the application's *behavioral core*: no repository CRUD method, no service method, and no controller action is invoked anywhere in the test suite. Coverage is broad at the edges and absent in the middle.

---

## 2. Target Mapping (condensed)

Full table: `transitions/01-test-mapping-20260917-120000.md`.

| Group | Source units | Mapped | Unmapped |
|-------|--------------|--------|----------|
| PilotApi.Domain | 12 logic-bearing (+20 interfaces) | 11 | AboutResponse.cs |
| PilotApi.Repositories | 20 logic-bearing (+11 interfaces) | 20 | — |
| PilotApi.Services | 12 logic-bearing (+1 interface) | 12 | — |
| PilotApi.Shared | 37 logic-bearing (+9 interfaces, −5 constants) | 26 | HttpVerbRoleRequirement (marker, 0 paths), LowercaseParameterTransformer, OpenTelemetryConfiguration, SecurityHelper, ControllerAttributes (0 paths), OpenTelemetryExtensions |
| PilotApi.Web | 12 | 11 | Program.cs |

Orphan/example suites: `test/PilotApi.Domain.Tests/ExampleTest.cs` (vacuous `Assert.Pass`). One-to-many: `ServiceBase.cs` has two same-named suites (both reflection-only).

---

## 3. Coverage Matrix

Per-path detail: `transitions/03-assertion-audit-20260917-120000.md`. Unit-level roll-up:

| Source unit (under `src/`) | Paths | Covered | Partially | Uncovered |
|----------------------------|-------|---------|-----------|-----------|
| PilotApi.Domain — 8 DTOs + 4 Responses | 22 | 18 | 0 | 4 |
| ↳ AboutResponse, AddResponseInt/String.ToString, RetrieveResponse.ToString | 4 | 0 | 0 | 4 |
| PilotApi.Repositories — EntityBase + 8 entities | 10 | 6 | 2 | 2 |
| PilotApi.Repositories — DataSourceContext | 24 | 14 | 0 | 10 |
| PilotApi.Repositories — EntityUpdateHandler | 39 | 23 | 0 | 16 |
| PilotApi.Repositories — **RepositoryBase** | 59 | 0 | 1 | **58** |
| PilotApi.Repositories — 8 Dapper repositories | 88 | 0 | 40 | 48 |
| PilotApi.Repositories — UserRolesRepository | 4 | 4 | 0 | 0 |
| PilotApi.Services — **ServiceBase** | 15 | 0 | 0 | **15** |
| PilotApi.Services — 8 concrete services | 8 | 8 | 0 | 0 |
| PilotApi.Services — DataMapperHandler | 56 | 24 | 0 | 32 |
| PilotApi.Services — ServicesInjectionExtensions | 4 | 1 | 1 | 2 |
| PilotApi.Services — PreferredUsernameRoleClaimsTransformation | 5 | 5 | 0 | 0 |
| PilotApi.Shared — ApiExtensions | 10 | 3 | 1 | 6 |
| PilotApi.Shared — UnhandledExceptionMiddleware | 7 | 3 | 0 | 4 |
| PilotApi.Shared — BypassOnInactiveAuthorizationMiddlewareResultHandler | 8 | 6 | 0 | 2 |
| PilotApi.Shared — HttpVerbRoleAuthorizationHandler | 5 | 4 | 0 | 1 |
| PilotApi.Shared — LowercaseParameterTransformer | 2 | 0 | 0 | 2 |
| PilotApi.Shared — SqlBuilder | 56 | 19 | 6 | 31 |
| PilotApi.Shared — Configuration family (8 classes) | 93 | 80 | 1 | 12 |
| PilotApi.Shared — 2 Exceptions | 6 | 6 | 0 | 0 |
| PilotApi.Shared — **SecurityHelper** | 49 | 0 | 0 | **49** |
| PilotApi.Shared — Logging (3 units) | 8 | 6 | 2 | 0 |
| PilotApi.Shared — OpenApiExtensions | 7 | 2 | 1 | 4 |
| PilotApi.Shared — 4 OpenApi transformers | 29 | 4 | 0 | 25 |
| PilotApi.Shared — OpenTelemetryExtensions | 5 | 0 | 0 | 5 |
| PilotApi.Shared — SwaggerExtensions | 2 | 1 | 1 | 0 |
| PilotApi.Web — SimpleControllerBase | 0 | — | — | — |
| PilotApi.Web — SystemController | 8 | 5 | 0 | 3 |
| PilotApi.Web — **8 V1 controllers** | 112 | 8 | 0 | **104** |
| PilotApi.Web — ApplicationInjectionExtensions | 2 | 1 | 0 | 1 |
| PilotApi.Web — Program.cs | 2 | 0 | 0 | 2 |

---

## 4. Problem Register (by severity)

### Critical — 136 untested error/exception paths

| Where | What |
|-------|------|
| `src/PilotApi.Repositories/Repositories/Base/RepositoryBase.cs` | All 34 throw/catch paths untested: `BuildParameters` guards (l.152–162); `CountAllAsync` catch (l.203); `DeleteAsync` guard + catch (l.227, 264); `GetAllAsync`/`GetAsync` catches (l.307, 353); `InsertAsync` guard, unhandled-type throw, catch (l.380, 437, 459); 4 query methods' guards + catches (l.478–630); `UpdateAsync` guard + catch (l.663, 697); `GetNextIdsAsync` 3 InvalidOperationException arms + catch (l.747–787); `Validate` 8 ConfigurationException arms (l.806–856) |
| `src/PilotApi.Repositories/Repositories/*.cs` (8 files) | `default:` InvalidOperationException arms of `ColumnNames`/`KeyColumnNames`/`TableName` switches (24 paths) — only SqlServer arm ever executes |
| `src/PilotApi.Services/Services/Base/ServiceBase.cs` | 7 guard clauses (`ids.Length < 1`, `model == null`, `string.IsNullOrWhiteSpace(query)`) never triggered |
| `src/PilotApi.Web/Controllers/V1/*.cs` (8 controllers) | 40 `IsError → BadRequest + Warning header` arms (5 per controller) never executed — controllers are only constructed, never called |
| `src/PilotApi.Shared/Builders/SqlBuilder.cs` | 16 throw arms untested: `BuildGetNextId` unknown/other-source, `BuildInsert` keys-empty/other-source, both `BuildSelect` guard sets + paged `page<0`/`pageSize<1`/keys-missing/other-source, `BuildUpdate` guards, `BuildWhereClause` guard, `BuildConnectionString` unhandled-source |
| `src/PilotApi.Shared/Helpers/SecurityHelper.cs` | ctor guard; JWT `CanReadToken` failure fallbacks in `GetJwtClaims`/`CleanJwtToken` |
| `src/PilotApi.Shared/Api/Middleware/UnhandledExceptionMiddleware.cs` | ctor guards (null next/logger); `UserException && !HasStarted → 500 with user message` arm |
| `src/PilotApi.Shared/Api/Security/HttpVerbRoleAuthorizationHandler.cs` | empty Authorization header → Fail arm |
| `src/PilotApi.Shared/Configuration/ApplicationConfiguration.cs` | OpenTelemetry-null arm; multi-error → `AggregateException` arm |
| `src/PilotApi.Repositories/DataSource/DataSourceContext.cs` | no-active-connection arm (`First()` throws InvalidOperationException; documented ConfigurationException is **dead code** at l.54–58) |
| `src/PilotApi.Repositories/Handlers/EntityUpdateHandler.cs` | unknown-entity throw (l.51–54) — test with this name **never triggers the throw** (false positive) |
| `src/PilotApi.Services/Extensions/ServicesInjectionExtensions.cs` | `ServicesConfiguration` null-builder guard |
| `src/PilotApi.Shared/Api/Extensions/ApiExtensions.cs` | `MapControllers` InvalidOperationException catch-swallow |
| `src/PilotApi.Shared/OpenTelemetry/Extensions/OpenTelemetryExtensions.cs` | null-builder guard (no suite at all) |
| `src/PilotApi.Web/Program.cs` | top-level catch → `Log.Fatal` |

### High — 191 untested core decision branches

| Where | What |
|-------|------|
| `src/PilotApi.Services/Services/Base/ServiceBase.cs` | All 8 happy paths (repository delegation + DTO mapping orchestration) |
| `src/PilotApi.Web/Controllers/V1/*.cs` | 40 happy-path responses (Ok / CreatedAtAction / NoContent) across 8 controllers |
| `src/PilotApi.Shared/Helpers/SecurityHelper.cs` | Entire JWT claims pipeline: `GetJwtClaims` realm_access/resource_access/scope/scp arms, `GetJwtPrincipal` identity merge arms, `AddRealmRoles`/`AddResourceRoles`/`AddScopes`, `HandleOnAuthenticationFailed` expired-vs-other, AudienceValidator lambda |
| `src/PilotApi.Repositories/Repositories/*.cs` | 24 PostgreSQL switch arms (ColumnNames/KeyColumnNames/TableName × 8 repos) |
| `src/PilotApi.Repositories/Repositories/Base/RepositoryBase.cs` | 22 happy/decision paths: all CRUD success arms, InsertAsync CreateKey/id-typing arms, GetNextIdsAsync PostgreSQL arms, Validate pass |
| `src/PilotApi.Shared/Builders/SqlBuilder.cs` | 12 dialect/format arms: PostgreSQL connection-string/RETURNING/LIMIT-OFFSET, composite-key SELECT 1, WHERE-clause append, auto-key skip in SET |
| `src/PilotApi.Services/Handlers/DataMapperHandler.cs` | 7 asymmetric dispatch arms (Customers/OrderDetails/Products/Suppliers Dto→Entity; Employees/Orders/Shippers Entity→Dto) |
| `src/PilotApi.Shared/OpenApi/Transformers/GlobalOperationTransformer.cs` | All 11 TransformAsync arms + RequiresSecurity matrix (suite is instantiation-only) |
| `src/PilotApi.Shared/OpenApi/Extensions/OpenApiExtensions.cs` | Per-version registration loop + controller/version matching (tests fail-fast before the loop) |
| `src/PilotApi.Shared/OpenApi/Transformers/DocumentInfoTransformer.cs`, `ManualXmlCommentsOperationTransformer.cs` | TransformAsync bodies + GetXmlMemberKey never invoked |
| `src/PilotApi.Shared/Api/Extensions/ApiExtensions.cs` | ApiWebApplication dev/non-dev arms + happy pipeline; ApiWebApplicationBuilder happy path (**valid-builder test commented out**); UseSecurity happy path |
| `src/PilotApi.Web/Controllers/SystemController.cs` | Healthcheck; About PostgreSQL-suffix arm |
| `src/PilotApi.Shared/OpenTelemetry/Extensions/OpenTelemetryExtensions.cs` | happy path + IsDevelopment console-exporter arms |
| misc | ServicesInjectionExtensions/ServicesConfiguration happy; ApplicationInjectionExtensions happy; Program.cs happy |

### Medium — 76 (incl. 2 latent mock-bypass risks)

Untested boundaries: `DataSourceContext` no-op Commit/Rollback arms, already-open/already-transacting GetConnection arms, 4 Dispose(bool) arms; `EntityUpdateHandler` 7 key-no-match arms; controllers' `Result == null → NotFound` (16) and `Result <= 0 / empty → BadRequest` (8) arms; `SecurityHelper` blank/short-token/redaction boundaries (16); `SqlBuilder` Initialize enum-resolution arm + paged boundaries; `BuildWarningMessage` empty-reasons + authenticate-failure fallbacks; `RetrieveResponse`-style null arms in `LowercaseParameterTransformer`, `SecurityConfiguration.Authority` null-BaseUrl, `OpenTelemetryConfiguration` boundaries; `ApplicationConfiguration`/`About` no-active-connection arms; `MapEntityToDtoListAsync` empty-list boundary; **MB-01/MB-02** latent mock-bypass infrastructure.

### Low — 94

Weak/superficial assertions on executed paths: repository spy suites assert only `IsNotNull`/`IsNotEmpty` on the SqlServer arms (40); `DoesNotThrow`-only happy paths in Logging/Swagger/OpenApi/Api extension suites (5); unasserted `ToString()` executions via `Console.WriteLine` (4+); dominated/unreachable cast-fail arms in `DataMapperHandler` converters (16+1) and `EntityUpdateHandler` updaters (8); Domain `ToString()` gaps (3); misc reflection-only suite content.

---

## 5. Remediation Suggestions (Given/When/Then skeletons — review only, never applied)

**RepositoryBase CRUD + error handling** (`src/PilotApi.Repositories/Repositories/Base/RepositoryBase.cs`) — use a fake `IDbConnection`/Dapper-interception double or an in-memory provider:

```gherkin
Given a repository whose data source ExecuteAsync returns 0 rows
When DeleteAsync is called with one id
Then RollbackAsync is called once, CommitAsync is never called,
  and the response is IsError=true with "Zero rows were deleted" message
```

```gherkin
Given a repository whose connection throws on ExecuteAsync
When UpdateAsync is called with a valid model
Then the exception propagates, RollbackAsync is called, and the error is logged
```

```gherkin
Given ColumnNames has fewer items than EntityColumns
When any repository method is called
Then Validate throws ConfigurationException mentioning item counts
```

**ServiceBase guards + orchestration** (`src/PilotApi.Services/Services/Base/ServiceBase.cs`):

```gherkin
Given a CategoriesService whose repository returns a RetrieveResponse with an entity
When GetByIdAsync is called with one id
Then the IDataMapperHandler maps the entity and the response carries the mapped DTO
  and the repository error message is propagated
```

```gherkin
Given a CategoriesService
When DeleteAsync is called with an empty ids array
Then ArgumentException is thrown and the repository is never called
```

**Controller actions** (`src/PilotApi.Web/Controllers/V1/CategoriesController.cs` and family):

```gherkin
Given ICategoriesService.GetAllAsync returns IsError with message "db down"
When GetAll is invoked
Then the result is BadRequest and the response Warning header equals "db down"
```

```gherkin
Given ICategoriesService.GetByIdAsync returns Result = null
When GetById is invoked with id 7
Then the result is NotFound
```

```gherkin
Given ICategoriesService.InsertAsync returns Result = 5
When Add is invoked with a valid model
Then the result is CreatedAtAction targeting GetById with categoryId 5
  and the body is AddResponseInt with Id 5
```

**SecurityHelper** (`src/PilotApi.Shared/Helpers/SecurityHelper.cs`) — highest-value untested unit (49 paths):

```gherkin
Given a Keycloak-style JWT payload with realm_access.roles ["a","b"] and a space-separated scope
When GetJwtClaims processes the bearer header
Then role claims a and b and one claim per scope are emitted, plus the raw realm_access claim
```

```gherkin
Given an Authorization header that does not start with "Bearer "
When BearerTokenClean is called
Then the whole value is redacted via SecurityUtilities.Redact
```

**SqlBuilder PostgreSQL + paging arms** (`src/PilotApi.Shared/Builders/SqlBuilder.cs`) — parameterize the fixture by DataSourceTypes.PostgreSQL:

```gherkin
Given a SqlBuilder initialized for PostgreSQL
When BuildSelect is called with page 2, pageSize 20 and a key column
Then the SQL contains "ORDER BY", "LIMIT 20 OFFSET 20"
```

```gherkin
Given a SqlBuilder for an auto-increment single-key table on PostgreSQL
When BuildInsert is called
Then the SQL ends with " RETURNING <key>;" instead of SCOPE_IDENTITY
```

**Repository switch arms**: given `DataSourceEnum = PostgreSQL` in the spy fixture, when `ColumnNames`/`KeyColumnNames`/`TableName` are read, then the lowercase PostgreSQL identifiers are returned; given an unrecognized enum, then reading any of them throws InvalidOperationException.

**EntityUpdateHandler throw arm** — fix the false-positive test:

```gherkin
Given an EntityUpdateHandler and an EntityBase subclass that is not one of the 8 known entities
When Update is called
Then InvalidOperationException is thrown mentioning "No mapper was found"
```

**UnhandledExceptionMiddleware**: given next throws UserException and the response has NOT started, when InvokeAsync runs, then the response is 500 JSON whose detail equals the UserException message; given next completes normally, then the response is untouched.

**LowercaseParameterTransformer / OpenTelemetryConfiguration / OpenTelemetryExtensions / SecurityUtilities.Redact / AboutResponse / *ToString gaps*: straightforward value-in/value-out cases (null/empty/boundary inputs, exact expected outputs).

---

## 6. Excluded Suites

| Suite | Reason |
|-------|--------|
| `test/Bruno/PilotApiDotNet/` (opencollection.yml) | E2E/manual HTTP request collection — not executable unit tests |
| `test/PilotApi.Architecture.Tests/` (9 files: AsyncNaming, Attributes, BannedDependency, BaseClass, Controller, Cycle, DependencyInversion, LayerDependency, NamingConvention) | ArchUnitNET whole-solution convention rules — not 1:1 source-unit tests; they complement (and partially substitute for) unit coverage of structural rules |
| `test/PilotApi.TestingShared/` (4 files), `test/PilotApi.Web.Tests/TestDoubles/` (2), `test/PilotApi.Repositories.Tests/Testing/` (9) | Shared fixtures/doubles/spies — infrastructure, not suites |
| `test/PilotApi.Domain.Tests/ExampleTest.cs` | Orphan: vacuous `Assert.Pass` template test |

---

## 7. Notable code findings surfaced by the audit (informational)

1. `DataSourceContext..ctor` and `SqlBuilder.Initialize`: `.First(f => f.Active)` makes the following null-check dead code; a missing active connection throws `InvalidOperationException`, not the documented `ConfigurationException`.
2. `DataSourceConfiguration.Validate` throws `ConfigurationException` immediately on an unrecognized `DataSourceType`, unlike sibling validators that collect into the `exceptions` list.
3. `OpenApiConfiguration.Initialize` copies the `Contact` reference (shallow) — pinned by a `SameAs` assertion in tests, so the behavior is at least documented by the suite.
4. `ApiExtensionsTests.ApiWebApplicationBuilder_WithValidBuilder_ShouldRegisterServices_Test` is commented out, leaving the main builder happy path untested.
5. `EntityUpdateHandlerTests...UnknownEntityType_ThrowsInvalidOperationException_Test` name/body mismatch (see Problem Register).
